---
phase: 04-subtitle-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/downloader.js, src/index.js]
autonomous: true
requirements: ["링크 입력 시 썸네일과 자막 텍스트를 미리보기로 응답하는 API 및 개별 다운로드 로직 구현"]
must_haves:
  truths:
    - "입력된 URL의 썸네일, 제목, 그리고 자막(텍스트) 데이터를 반환하는 미리보기용 API가 존재함"
    - "영상만 다운로드, 자막만 다운로드를 각각 수행할 수 있도록 백엔드 라우터가 분리/수정됨"
  artifacts:
    - path: "src/downloader.js"
      provides: "메타데이터/자막 추출 함수 및 개별 다운로드 함수"
  key_links: []
---

<objective>
단순 일괄 다운로드가 아닌, 1단계(정보 불러오기)와 2단계(선택 다운로드)로 나뉜 차별화된 UX를 제공하기 위해 백엔드를 전면 개편합니다.

Purpose: 사용자가 다운로드 전 영상의 썸네일을 확인하고, 자막을 텍스트로 미리 읽어본 후 원하는 것만 개별적으로 다운로드할 수 있게 하기 위함입니다.
Output: 메타데이터(썸네일, 제목, 자막텍스트) 제공 API (`/api/info`) 및 각각의 다운로드 API 구현.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: downloader.js 확장 (미리보기 및 분리 다운로드)</name>
  <files>src/downloader.js</files>
  <read_first>src/downloader.js</read_first>
  <action>`src/downloader.js`를 수정합니다. 1. `getVideoInfo`를 고도화하여 썸네일 URL을 확실하게 리턴하도록 합니다. 2. 영상을 다운로드하지 않고 자막 파일만 빠르게 추출(skip-download, write-subs)하여 읽어오는 `getSubtitlesText` 함수를 구현합니다. 3. `downloadSubtitle` 전용 함수를 별도로 구현합니다.</action>
  <verify>grep getSubtitlesText src/downloader.js exits 0</verify>
  <acceptance_criteria>
    - 썸네일 메타데이터와 자막 텍스트를 파싱하는 로직이 완성됨
  </acceptance_criteria>
  <done>백엔드 추출 로직 고도화 완료</done>
</task>

<task type="auto">
  <name>Task 2: 라우터 분리 및 추가</name>
  <files>src/index.js</files>
  <read_first>src/index.js</read_first>
  <action>`src/index.js`에 새로운 `POST /api/info` 라우터를 생성하여 썸네일 URL과 자막 텍스트를 JSON으로 반환하도록 합니다. 그리고 다운로드 라우터를 분리하거나 `type` 파라미터를 받아 영상 다운로드와 자막 다운로드(`.srt` 또는 `.vtt`)를 각각 응답할 수 있도록 라우팅을 개편합니다.</action>
  <verify>grep /api/info src/index.js exits 0</verify>
  <acceptance_criteria>
    - 미리보기 정보 API 엔드포인트와 분리된 다운로드 로직이 존재함
  </acceptance_criteria>
  <done>백엔드 API 구조 개편 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] /api/info 로 요청 시 썸네일 링크와 자막 텍스트가 정상적으로 반환됨을 확인
</verification>

<success_criteria>
- 백엔드가 2-Step 프로세스를 완벽히 지원함
</success_criteria>
