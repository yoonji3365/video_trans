---
phase: 04-subtitle-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/downloader.js, src/index.js]
autonomous: true
requirements: ["유튜브 영상에서 자막 파일을 추출하여 제공하는 백엔드 로직 구현"]
must_haves:
  truths:
    - "자막 추출을 지원하는 로직이 서버에 구현되어 있음"
  artifacts:
    - path: "src/downloader.js"
      provides: "자막 추출 파라미터가 추가된 yt-dlp 로직"
  key_links: []
---

<objective>
`yt-dlp`를 활용하여 유튜브 영상에 등록된 자동 생성 또는 공식 자막(Subtitles)을 추출할 수 있도록 백엔드를 확장합니다.

Purpose: 영상에 포함된 음성을 텍스트로 바로 확인할 수 있도록 편의를 제공하기 위함입니다.
Output: 자막이 존재할 경우 `.vtt` 혹은 `.srt` 형태의 자막 파일을 영상과 함께 (혹은 단독으로) 처리할 수 있는 백엔드 API 기능.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: yt-dlp 자막 추출 옵션 추가</name>
  <files>src/downloader.js</files>
  <read_first>src/downloader.js</read_first>
  <action>`src/downloader.js`의 `downloadVideo` 함수에 자막 추출 관련 `yt-dlp` 옵션(`writeAutoSubs: true`, `writeSubs: true`, `subFormat: 'srt/vtt/best'`)을 추가합니다.</action>
  <verify>grep writeSubs src/downloader.js exits 0</verify>
  <acceptance_criteria>
    - downloader.js 코드가 자막 파일을 디스크에 생성할 수 있음
  </acceptance_criteria>
  <done>백엔드 자막 추출기능 모듈화 완료</done>
</task>

<task type="auto">
  <name>Task 2: API 엔드포인트에서 자막 파일 응답 처리</name>
  <files>src/index.js</files>
  <read_first>src/index.js</read_first>
  <action>`src/index.js`의 `/api/convert` 라우터에서 클라이언트가 자막 다운로드를 요청했는지 확인하는 옵션(예: `req.body.includeSubtitles`)을 받습니다. 자막을 요청했다면 영상 파일과 자막 파일을 압축(zip)하여 보내거나 각각 처리하는 방식을 구현합니다.</action>
  <verify>grep includeSubtitles src/index.js exits 0</verify>
  <acceptance_criteria>
    - 클라이언트 요청에 따라 자막 파일을 다운로드 받을 수 있는 API 처리 완료
  </acceptance_criteria>
  <done>자막 전송 API 연동 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] 자막 옵션을 켰을 때 정상적으로 자막 파일이 떨어지는지 테스트 확인
</verification>

<success_criteria>
- 백엔드에서 자막 추출 기능이 에러 없이 작동함
</success_criteria>
