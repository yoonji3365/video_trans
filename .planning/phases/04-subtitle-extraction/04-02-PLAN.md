---
phase: 04-subtitle-extraction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [public/index.html, public/index.css, public/app.js]
autonomous: true
requirements: ["URL 입력 후 썸네일과 자막 텍스트를 보여주고 각각 다운로드 버튼을 제공하는 모던한 미리보기 UI 구현"]
must_haves:
  truths:
    - "데이터 로드 후 화면이 부드럽게 확장되며 좌/우 혹은 상/하로 썸네일 영역과 자막 영역이 나타남"
    - "썸네일 영역 상단에 [영상 다운받기], 자막 텍스트 영역 상단에 [자막 다운받기] 버튼이 존재함"
  artifacts:
    - path: "public/index.html"
      provides: "새로운 2-Step 미리보기 레이아웃"
  key_links: []
---

<objective>
기존의 단순 "변환" 폼을 넘어, 썸네일 확인 및 자막 내용을 화면에서 직접 읽고 개별 다운로드할 수 있는 혁신적이고 아름다운 대시보드 형태의 UI를 구축합니다.

Purpose: 타 서비스들과 압도적인 차별성을 갖는 사용자 경험(UX)을 달성합니다.
Output: 프론트엔드 정보 표시 카드 영역 및 다중 다운로드 인터페이스.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: HTML/CSS 미리보기 카드 UI 디자인</name>
  <files>public/index.html, public/index.css</files>
  <read_first>public/index.html, public/index.css</read_first>
  <action>`index.html`에 검색 결과가 노출될 숨겨진 컨테이너(preview-container)를 만듭니다. 이 안에는 두 구역이 들어갑니다. 구역 1: [영상 다운받기] 버튼과 고해상도 썸네일 이미지. 구역 2: [자막 다운받기] 버튼과 길게 스크롤되는 자막 텍스트 박스. `index.css`를 수정하여 글래스모피즘 테마에 어울리는 고급스럽고 반응형(모바일에서는 세로 배치, PC에서는 가로 배치)인 디자인을 적용합니다.</action>
  <verify>grep preview-container public/index.html exits 0</verify>
  <acceptance_criteria>
    - 썸네일 뷰어와 자막 뷰어 레이아웃이 아름답게 구축됨
  </acceptance_criteria>
  <done>새로운 프론트엔드 레이아웃 구축 완료</done>
</task>

<task type="auto">
  <name>Task 2: 프론트엔드 API 연동 (정보 불러오기 및 다운로드 분리)</name>
  <files>public/app.js</files>
  <read_first>public/app.js</read_first>
  <action>`app.js`의 작동 방식을 변경합니다. 폼 제출 시 기존의 변환 API가 아닌 `/api/info`로 요청하여 정보를 불러옵니다. 응답을 받으면 `preview-container`를 렌더링하고, 영상 다운받기 버튼과 자막 다운받기 버튼 클릭 시 각각 백엔드에 다운로드 요청을 보내 파일을 받아오도록 이벤트를 바인딩합니다.</action>
  <verify>grep /api/info public/app.js exits 0</verify>
  <acceptance_criteria>
    - 정보 조회와 개별 다운로드 기능이 완벽하게 분리 작동함
  </acceptance_criteria>
  <done>프론트엔드 비즈니스 로직 연동 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] 링크 입력 후 썸네일과 자막 내용이 화면에 예쁘게 나오는지 확인
- [ ] 두 가지 다운로드 버튼이 각각 정상 작동하는지 확인
</verification>

<success_criteria>
- 시장의 어떤 유튜브 변환기보다 세련되고 기능적인 UI 완성
</success_criteria>
