---
phase: 04-subtitle-extraction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [public/index.html, public/index.css, public/app.js]
autonomous: true
requirements: ["사용자가 자막 포함 여부를 선택할 수 있는 UI 옵션 추가"]
must_haves:
  truths:
    - "프론트엔드 폼에 자막 다운로드 토글/체크박스가 있음"
  artifacts:
    - path: "public/index.html"
      provides: "자막 옵션 UI 요소"
  key_links: []
---

<objective>
사용자가 다운로드 버튼을 누르기 전, 영상과 함께 자막도 다운로드 받을 것인지 선택할 수 있는 예쁜 스위치(Toggle) UI를 추가합니다.

Purpose: 불필요한 자막까지 강제로 다운받게 하지 않고, 사용자가 능동적으로 옵션을 선택할 수 있게 하여 사용자 경험(UX)을 높입니다.
Output: 프론트엔드에 "자막 함께 다운로드" 옵션 추가.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 프론트엔드 토글 UI 구현</name>
  <files>public/index.html, public/index.css</files>
  <read_first>public/index.html, public/index.css</read_first>
  <action>`index.html`의 다운로드 버튼 위에 "자막 포함하기" 체크박스 또는 토글 스위치를 추가합니다. `index.css`를 수정하여 기존의 모던한 글래스모피즘 테마와 잘 어울리도록 세련되게 스타일링합니다.</action>
  <verify>grep checkbox public/index.html exits 0</verify>
  <acceptance_criteria>
    - 화면에 직관적인 자막 선택 UI가 존재함
  </acceptance_criteria>
  <done>자막 토글 UI 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: API 요청 시 자막 옵션 전달</name>
  <files>public/app.js</files>
  <read_first>public/app.js</read_first>
  <action>`app.js`에서 폼 제출 시 사용자가 체크박스를 체크했는지 확인하고, `fetch()` 요청의 JSON `body`에 `{ url, includeSubtitles: true/false }` 형태로 담아 서버로 전달합니다.</action>
  <verify>grep includeSubtitles public/app.js exits 0</verify>
  <acceptance_criteria>
    - 자막 옵션 값이 백엔드 API로 올바르게 전달됨
  </acceptance_criteria>
  <done>프론트엔드 데이터 연동 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] 체크박스를 켜고 다운로드를 요청했을 때 자막 파일이 정상적으로 엮어서 내려오는지 확인
</verification>

<success_criteria>
- 사용자가 스위치 한 번으로 영상과 자막을 동시에 얻을 수 있음
</success_criteria>
