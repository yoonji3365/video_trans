---
phase: 02-api-frontend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/index.js, public/index.html, public/index.css]
autonomous: true
requirements: ["사용자가 유튜브 URL을 입력하고 변환 버튼을 누를 수 있는 인터페이스 제공"]
must_haves:
  truths:
    - "Express 서버가 public 폴더의 정적 파일을 제공함"
    - "아름답고 모던한 UI가 구현됨"
  artifacts:
    - path: "public/index.html"
      provides: "기본 화면 구조"
    - path: "public/index.css"
      provides: "화려한 UI/UX 디자인 및 스타일링"
  key_links: []
---

<objective>
Express에서 정적 프론트엔드 파일을 제공하도록 설정하고, 화려하고 모던한 UI 디자인 시스템을 구축합니다.

Purpose: 유튜브 MP3 변환기 사용자를 위해 매력적이고 직관적인 화면을 제공하기 위함입니다.
Output: 아름답게 스타일링된 정적 웹페이지를 호스팅하는 Express 서버.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Express에서 정적 파일 제공 기능 추가</name>
  <files>src/index.js</files>
  <read_first>src/index.js</read_first>
  <action>`src/index.js`에 `app.use(express.static(path.join(__dirname, '../public')));` 코드를 추가하여 `public` 폴더 내의 정적 파일들을 서비스하도록 설정합니다.</action>
  <verify>grep "express.static" src/index.js exits 0</verify>
  <acceptance_criteria>
    - src/index.js 파일 내에 "express.static" 코드가 존재함
  </acceptance_criteria>
  <done>Express가 프론트엔드 화면을 띄워줄 준비가 됨</done>
</task>

<task type="auto">
  <name>Task 2: 화려한 UI 기본 틀 생성</name>
  <files>public/index.html, public/index.css</files>
  <read_first></read_first>
  <action>`public` 폴더를 생성합니다. 그 안에 모던한 글래스모피즘(Glassmorphism) 디자인이 적용된 `index.html`을 생성합니다. 화면 정중앙에 H1 타이틀, 안내 문구, 유튜브 URL 입력창, "MP3로 변환" 버튼이 들어간 카드를 배치합니다. `index.css`를 만들어 화려한 미적 요소를 적용합니다: 세련된 다크 모드에 생동감 있는 그라데이션 포인트 추가, Google Fonts('Inter' 등) 적용, 버튼 호버 시 부드러운 애니메이션 효과, 모바일 환경을 고려한 반응형 레이아웃을 구현합니다.</action>
  <verify>grep "변환" public/index.html exits 0</verify>
  <acceptance_criteria>
    - public/index.html 파일에 입력창과 버튼 요소가 존재함
    - public/index.css 파일에 그라데이션 배경 및 hover 애니메이션 코드가 존재함
  </acceptance_criteria>
  <done>프론트엔드 화면의 HTML 구조와 아름다운 디자인 파일 생성 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] index.html이 정상적으로 열리며 화려한 스타일이 제대로 적용되어 있음
</verification>

<success_criteria>
- 모든 태스크 완료
</success_criteria>
