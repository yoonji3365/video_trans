---
phase: 02-api-frontend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/index.js, public/index.html, public/index.css]
autonomous: true
requirements: ["User interface with an input field and conversion button"]
must_haves:
  truths:
    - "Express serves static files from public directory"
    - "A beautiful, modern UI is present"
  artifacts:
    - path: "public/index.html"
      provides: "Main structure"
    - path: "public/index.css"
      provides: "Rich aesthetics and styling"
  key_links: []
---

<objective>
Serve a static frontend from Express and establish a rich, modern UI design system.

Purpose: Provide a stunning, user-friendly interface for the YouTube to MP3 converter.
Output: An Express server that hosts a beautifully styled static webpage.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Serve static files in Express</name>
  <files>src/index.js</files>
  <read_first>src/index.js</read_first>
  <action>Add `app.use(express.static(path.join(__dirname, '../public')));` to `src/index.js` to serve static files from the `public` directory.</action>
  <verify>grep "express.static" src/index.js exits 0</verify>
  <acceptance_criteria>
    - src/index.js contains "express.static"
  </acceptance_criteria>
  <done>Express is configured to serve frontend</done>
</task>

<task type="auto">
  <name>Task 2: Create rich UI foundation</name>
  <files>public/index.html, public/index.css</files>
  <read_first></read_first>
  <action>Create a `public` directory. Inside it, create `index.html` with a modern, glassmorphism design. Include a centered card containing an H1 title, a descriptive text, an input field for the YouTube URL, and a "Convert to MP3" button. Create `index.css` applying rich aesthetics: use a sleek dark mode with vibrant gradient accents, Google Fonts (e.g., 'Inter'), smooth transitions for button hovers, and a responsive layout.</action>
  <verify>grep "Convert" public/index.html exits 0</verify>
  <acceptance_criteria>
    - public/index.html contains an input field and button
    - public/index.css contains gradient backgrounds and hover states
  </acceptance_criteria>
  <done>Frontend UI skeleton and styles created</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] index.html opens properly with styles applied
</verification>

<success_criteria>
- All tasks completed
</success_criteria>
