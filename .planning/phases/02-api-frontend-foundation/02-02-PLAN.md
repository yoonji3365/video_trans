---
phase: 02-api-frontend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [public/index.html, public/app.js]
autonomous: true
requirements: ["Provide a download link for the MP3 file"]
must_haves:
  truths:
    - "Frontend calls /api/convert and triggers download"
  artifacts:
    - path: "public/app.js"
      provides: "Frontend API integration logic"
  key_links: []
---

<objective>
Connect the frontend UI to the backend `/api/convert` endpoint using vanilla JavaScript.

Purpose: Make the "Convert" button functional by sending the user's YouTube URL to the backend.
Output: A functional frontend that communicates with the server and downloads the MP3.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add JavaScript for API integration</name>
  <files>public/app.js, public/index.html</files>
  <read_first>public/index.html</read_first>
  <action>Include `app.js` in `index.html`. In `public/app.js`, add an event listener to the form/button. When clicked, read the input URL, send a POST request to `/api/convert` with `{ url }` as JSON using `fetch()`. Wait for the response (as a blob), create a temporary `a` tag to trigger the browser download of the MP3 file. (Note: Loading states and error handling will be fully fleshed out in Phase 3, but basic fetch logic must work here).</action>
  <verify>grep fetch public/app.js exits 0</verify>
  <acceptance_criteria>
    - public/app.js contains "fetch('/api/convert'"
    - public/app.js handles Blob response for file download
  </acceptance_criteria>
  <done>Frontend triggers conversion and download</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] app.js contains the fetch logic correctly
</verification>

<success_criteria>
- All tasks completed
</success_criteria>
