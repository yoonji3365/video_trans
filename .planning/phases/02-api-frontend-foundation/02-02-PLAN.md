---
phase: 02-api-frontend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [public/index.html, public/app.js]
autonomous: true
requirements: ["MP3 파일을 다운로드할 수 있도록 프론트엔드와 API를 연동"]
must_haves:
  truths:
    - "프론트엔드에서 /api/convert를 호출하고 정상적으로 파일이 다운로드 됨"
  artifacts:
    - path: "public/app.js"
      provides: "프론트엔드 API 연동 자바스크립트 로직"
  key_links: []
---

<objective>
바닐라 JavaScript를 사용하여 프론트엔드 UI를 백엔드의 `/api/convert` 엔드포인트와 연결합니다.

Purpose: 화면의 "변환" 버튼을 눌렀을 때 사용자가 입력한 유튜브 URL이 백엔드로 전송되어 실제 다운로드 기능을 하도록 만들기 위함입니다.
Output: 서버와 통신하고 MP3를 다운로드하는 기능이 활성화된 프론트엔드 화면.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: API 연동을 위한 JavaScript 추가</name>
  <files>public/app.js, public/index.html</files>
  <read_first>public/index.html</read_first>
  <action>`index.html`에 `app.js`를 연결합니다. `public/app.js` 파일을 만들고, 버튼/폼에 클릭 이벤트 리스너를 추가합니다. 클릭 시 입력된 URL 값을 읽어와 `fetch()`를 사용해 `/api/convert`로 `{ url }`을 JSON 형태로 담아 POST 요청을 보냅니다. 응답(Blob 객체)을 받으면 임시로 `a` 태그를 생성하여 브라우저에서 MP3 파일 다운로드가 시작되도록 트리거합니다. (참고: 로딩 애니메이션 및 구체적인 에러 처리는 Phase 3에서 완성할 것이지만, 기본적인 fetch 다운로드 로직은 무조건 정상 작동해야 합니다).</action>
  <verify>grep fetch public/app.js exits 0</verify>
  <acceptance_criteria>
    - public/app.js 파일에 "fetch('/api/convert'" 요청 코드가 존재함
    - public/app.js 파일이 파일 다운로드를 위해 Blob 응답을 처리하는 코드를 포함함
  </acceptance_criteria>
  <done>프론트엔드 변환 요청 및 다운로드 연동 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] app.js 파일에 올바른 fetch 로직이 포함되어 있음
</verification>

<success_criteria>
- 모든 태스크 완료
</success_criteria>
