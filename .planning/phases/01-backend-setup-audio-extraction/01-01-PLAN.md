---
phase: 01-backend-setup-audio-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/index.js]
autonomous: true
requirements: ["유효한 유튜브 URL을 입력으로 받음"]
must_haves:
  truths:
    - "서버가 에러 없이 시작됨"
    - "Health check 엔드포인트가 200 OK를 반환함"
  artifacts:
    - path: "package.json"
      provides: "프로젝트 의존성 패키지"
      contains: "\"express\""
    - path: "src/index.js"
      provides: "서버 진입점(Entry point)"
      contains: "app.listen"
  key_links: []
---

<objective>
Node.js와 Express를 사용하여 백엔드 프로젝트를 초기화합니다.

Purpose: 유튜브 MP3 변환을 처리할 API의 뼈대를 구축하기 위함입니다.
Output: 정상적으로 동작하는 Express 서버.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: package.json 초기화 및 Express 설치</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>`npm init -y`를 실행하고 `npm install express cors dotenv`를 설치합니다. 또한 `npm install -D nodemon`을 실행합니다. `package.json`에 `nodemon src/index.js`를 실행하는 `dev` 스크립트를 추가합니다.</action>
  <verify>grep express package.json exits 0</verify>
  <acceptance_criteria>
    - package.json 의존성에 "express"가 포함됨
    - package.json 스크립트에 "dev": "nodemon src/index.js"가 포함됨
  </acceptance_criteria>
  <done>package.json 설정이 완료됨</done>
</task>

<task type="auto">
  <name>Task 2: 기본 Express 서버 생성</name>
  <files>src/index.js</files>
  <read_first>src/index.js</read_first>
  <action>`src/index.js`를 생성하여 Express 앱을 설정합니다. 'express'와 'cors'를 불러옵니다. cors 미들웨어를 사용합니다. `{ status: 'ok' }`를 반환하는 GET `/api/health` 엔드포인트를 추가합니다. `app.listen(3001, () => console.log('Server running on port 3001'))`를 호출합니다.</action>
  <verify>node -e "require('express')" exits 0</verify>
  <acceptance_criteria>
    - src/index.js 파일에 "express()" 코드가 존재함
    - src/index.js 파일에 "/api/health" 코드가 존재함
    - src/index.js 파일에 "app.listen(3001" 코드가 존재함
  </acceptance_criteria>
  <done>Express 서버 생성 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev 명령어로 서버가 정상 작동함
- [ ] curl http://localhost:3001/api/health 호출 시 ok가 반환됨
</verification>

<success_criteria>
- 모든 태스크 완료
- Express 서버가 정상적으로 실행 중임
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-setup-audio-extraction/01-01-SUMMARY.md`
</output>
