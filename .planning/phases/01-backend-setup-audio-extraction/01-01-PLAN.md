---
phase: 01-backend-setup-audio-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, src/index.js]
autonomous: true
requirements: ["Accept valid YouTube URLs as input"]
must_haves:
  truths:
    - "Server starts up without errors"
    - "Health check endpoint returns 200 OK"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "\"express\""
    - path: "src/index.js"
      provides: "Entry point for the server"
      contains: "app.listen"
  key_links: []
---

<objective>
Initialize backend project with Node.js and Express.

Purpose: Set up the foundation for the API that will handle YouTube to MP3 conversions.
Output: Working Express server.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Initialize package.json and install Express</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Run `npm init -y` and `npm install express cors dotenv`. Also run `npm install -D nodemon`. Add a `dev` script in `package.json` to run `nodemon src/index.js`.</action>
  <verify>grep express package.json exits 0</verify>
  <acceptance_criteria>
    - package.json contains "express" in dependencies
    - package.json contains "dev": "nodemon src/index.js"
  </acceptance_criteria>
  <done>package.json is configured</done>
</task>

<task type="auto">
  <name>Task 2: Create basic Express server</name>
  <files>src/index.js</files>
  <read_first>src/index.js</read_first>
  <action>Create `src/index.js` setting up an Express app. Require 'express' and 'cors'. Use cors middleware. Add a GET `/api/health` endpoint returning `{ status: 'ok' }`. Call `app.listen(3001, () => console.log('Server running on port 3001'))`.</action>
  <verify>node -e "require('express')" exits 0</verify>
  <acceptance_criteria>
    - src/index.js contains "express()"
    - src/index.js contains "/api/health"
    - src/index.js contains "app.listen(3001"
  </acceptance_criteria>
  <done>Express server created</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run dev starts the server
- [ ] curl http://localhost:3001/api/health returns ok
</verification>

<success_criteria>
- All tasks completed
- Express server is running and healthy
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-setup-audio-extraction/01-01-SUMMARY.md`
</output>
