---
phase: 01-backend-setup-audio-extraction
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/downloader.js, src/index.js]
autonomous: true
requirements: ["Convert the extracted audio to MP3 format"]
must_haves:
  truths:
    - "Can download audio as MP3"
  artifacts:
    - path: "src/downloader.js"
      provides: "MP3 download function"
      contains: "extractAudio: true"
    - path: "src/index.js"
      provides: "Endpoint for conversion"
      contains: "/api/convert"
  key_links:
    - from: "src/index.js"
      to: "src/downloader.js"
      via: "imports downloader module"
      pattern: "require\\(.*downloader.*\\)"
---

<objective>
Implement MP3 conversion logic and expose it via an API endpoint.

Purpose: Allows the frontend to trigger the actual download and conversion process.
Output: A working `/api/convert` endpoint that returns an MP3 file.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add download logic to downloader module</name>
  <files>src/downloader.js</files>
  <read_first>src/downloader.js</read_first>
  <action>Export a function `downloadAudio(url, outputPath)` in `src/downloader.js`. It should call `youtubedl(url, { extractAudio: true, audioFormat: 'mp3', output: outputPath, noWarnings: true })`.</action>
  <verify>grep extractAudio src/downloader.js exits 0</verify>
  <acceptance_criteria>
    - src/downloader.js contains "extractAudio: true"
    - src/downloader.js contains "audioFormat: 'mp3'"
  </acceptance_criteria>
  <done>Audio download logic implemented</done>
</task>

<task type="auto">
  <name>Task 2: Add API endpoint</name>
  <files>src/index.js</files>
  <read_first>src/index.js, src/downloader.js</read_first>
  <action>In `src/index.js`, add a POST endpoint `/api/convert`. It expects `{ url }` in req.body (so add `express.json()` middleware). It should call `downloadAudio(url, tempFilePath)`. Upon completion, use `res.download(tempFilePath, 'audio.mp3')` to send the file. Use the `fs` and `path` modules to create a temporary file path in a `downloads` folder (create the folder if it doesn't exist).</action>
  <verify>grep "/api/convert" src/index.js exits 0</verify>
  <acceptance_criteria>
    - src/index.js contains "app.post('/api/convert'"
    - src/index.js contains "res.download"
  </acceptance_criteria>
  <done>API endpoint created</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] POST /api/convert handles invalid input gracefully
- [ ] endpoint exists and calls downloadAudio
</verification>

<success_criteria>
- All tasks completed
- Backend is ready to serve MP3 files
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-setup-audio-extraction/01-03-SUMMARY.md`
</output>
