---
phase: 01-backend-setup-audio-extraction
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/downloader.js, src/index.js]
autonomous: true
requirements: ["추출된 오디오를 MP3 포맷으로 변환함"]
must_haves:
  truths:
    - "오디오를 MP3 파일로 다운로드 할 수 있음"
  artifacts:
    - path: "src/downloader.js"
      provides: "MP3 다운로드 함수"
      contains: "extractAudio: true"
    - path: "src/index.js"
      provides: "변환 API 엔드포인트"
      contains: "/api/convert"
  key_links:
    - from: "src/index.js"
      to: "src/downloader.js"
      via: "다운로더 모듈 불러오기"
      pattern: "require\\(.*downloader.*\\)"
---

<objective>
MP3 변환 로직을 구현하고 이를 API 엔드포인트를 통해 노출합니다.

Purpose: 프론트엔드에서 실제 다운로드 및 변환 프로세스를 실행할 수 있도록 허용하기 위함입니다.
Output: MP3 파일을 반환해주는 정상 작동하는 `/api/convert` 엔드포인트.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 다운로더 모듈에 다운로드 로직 추가</name>
  <files>src/downloader.js</files>
  <read_first>src/downloader.js</read_first>
  <action>`src/downloader.js`에 `downloadAudio(url, outputPath)` 함수를 export 합니다. 이 함수는 `youtubedl(url, { extractAudio: true, audioFormat: 'mp3', output: outputPath, noWarnings: true })`를 실행해야 합니다.</action>
  <verify>grep extractAudio src/downloader.js exits 0</verify>
  <acceptance_criteria>
    - src/downloader.js 파일에 "extractAudio: true"가 존재함
    - src/downloader.js 파일에 "audioFormat: 'mp3'"가 존재함
  </acceptance_criteria>
  <done>오디오 다운로드 로직 구현 완료</done>
</task>

<task type="auto">
  <name>Task 2: API 엔드포인트 추가</name>
  <files>src/index.js</files>
  <read_first>src/index.js, src/downloader.js</read_first>
  <action>`src/index.js`에 POST 엔드포인트 `/api/convert`를 추가합니다. 이 API는 `req.body`에서 `{ url }`을 받습니다 (이를 위해 `express.json()` 미들웨어 적용). `downloadAudio(url, tempFilePath)`를 호출하고, 다운로드가 끝나면 `res.download(tempFilePath, 'audio.mp3')`를 사용하여 클라이언트에게 전송합니다. `fs` 및 `path` 모듈을 사용하여 `downloads` 폴더 내에 임시 파일을 저장하고(폴더가 없으면 생성), 전송이 완료되면 임시 파일을 삭제합니다.</action>
  <verify>grep "/api/convert" src/index.js exits 0</verify>
  <acceptance_criteria>
    - src/index.js 파일에 "app.post('/api/convert'" 코드가 존재함
    - src/index.js 파일에 "res.download" 코드가 존재함
  </acceptance_criteria>
  <done>API 엔드포인트 생성 및 파일 정리 로직 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] POST /api/convert가 잘못된 입력값에 대해서도 서버가 뻗지 않고 대응함
- [ ] 엔드포인트가 존재하며 downloadAudio를 정상 호출함
</verification>

<success_criteria>
- 모든 태스크 완료
- 백엔드가 MP3 파일을 서빙할 준비가 됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-setup-audio-extraction/01-03-SUMMARY.md`
</output>
