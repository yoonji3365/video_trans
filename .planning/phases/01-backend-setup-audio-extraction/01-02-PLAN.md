---
phase: 01-backend-setup-audio-extraction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [package.json, src/downloader.js]
autonomous: true
requirements: ["Extract audio from the provided YouTube video"]
must_haves:
  truths:
    - "Downloader module exists"
  artifacts:
    - path: "src/downloader.js"
      provides: "Audio extraction logic"
      contains: "youtube-dl-exec"
  key_links: []
---

<objective>
Integrate `youtube-dl-exec` (yt-dlp wrapper) to fetch video information and prepare for download.

Purpose: We need a reliable library to handle the complexities of extracting streams from YouTube.
Output: A downloader utility module.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install youtube-dl-exec</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Run `npm install youtube-dl-exec`.</action>
  <verify>grep youtube-dl-exec package.json exits 0</verify>
  <acceptance_criteria>
    - package.json contains "youtube-dl-exec"
  </acceptance_criteria>
  <done>Library installed</done>
</task>

<task type="auto">
  <name>Task 2: Create downloader module</name>
  <files>src/downloader.js</files>
  <read_first>src/downloader.js</read_first>
  <action>Create `src/downloader.js`. Import `youtubedl` from `youtube-dl-exec`. Export a function `getVideoInfo(url)` that calls `youtubedl(url, { dumpJson: true, noWarnings: true })` and returns the parsed JSON info.</action>
  <verify>grep youtube-dl-exec src/downloader.js exits 0</verify>
  <acceptance_criteria>
    - src/downloader.js contains "youtube-dl-exec"
    - src/downloader.js contains "dumpJson: true"
    - src/downloader.js exports getVideoInfo
  </acceptance_criteria>
  <done>Downloader module ready</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Module can be required without syntax errors
</verification>

<success_criteria>
- All tasks completed
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-setup-audio-extraction/01-02-SUMMARY.md`
</output>
