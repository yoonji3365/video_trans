---
phase: 01-backend-setup-audio-extraction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [package.json, src/downloader.js]
autonomous: true
requirements: ["입력된 유튜브 영상에서 오디오를 추출함"]
must_haves:
  truths:
    - "다운로더 모듈이 존재함"
  artifacts:
    - path: "src/downloader.js"
      provides: "오디오 추출 로직"
      contains: "youtube-dl-exec"
  key_links: []
---

<objective>
비디오 정보를 가져오고 다운로드를 준비하기 위해 `youtube-dl-exec` (yt-dlp 래퍼) 라이브러리를 연동합니다.

Purpose: 유튜브에서 스트림을 추출하는 복잡한 과정을 처리할 신뢰할 수 있는 라이브러리가 필요하기 때문입니다.
Output: 영상 정보를 추출하는 다운로더 유틸리티 모듈.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: youtube-dl-exec 설치</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>`npm install youtube-dl-exec`를 실행합니다.</action>
  <verify>grep youtube-dl-exec package.json exits 0</verify>
  <acceptance_criteria>
    - package.json 파일에 "youtube-dl-exec"가 존재함
  </acceptance_criteria>
  <done>라이브러리 설치 완료</done>
</task>

<task type="auto">
  <name>Task 2: 다운로더 모듈 생성</name>
  <files>src/downloader.js</files>
  <read_first>src/downloader.js</read_first>
  <action>`src/downloader.js`를 생성합니다. `youtube-dl-exec`에서 `youtubedl`을 불러옵니다. `youtubedl(url, { dumpJson: true, noWarnings: true })`를 호출하여 파싱된 JSON 영상 정보를 반환하는 `getVideoInfo(url)` 함수를 내보냅니다(export).</action>
  <verify>grep youtube-dl-exec src/downloader.js exits 0</verify>
  <acceptance_criteria>
    - src/downloader.js 파일에 "youtube-dl-exec" 코드가 존재함
    - src/downloader.js 파일에 "dumpJson: true" 옵션이 존재함
    - src/downloader.js 모듈이 getVideoInfo를 export 함
  </acceptance_criteria>
  <done>다운로더 모듈 준비 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] 구문 에러(syntax error) 없이 모듈을 정상적으로 require 할 수 있음
</verification>

<success_criteria>
- 모든 태스크 완료
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-setup-audio-extraction/01-02-SUMMARY.md`
</output>
