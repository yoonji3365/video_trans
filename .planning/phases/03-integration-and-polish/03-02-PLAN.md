---
phase: 03-integration-and-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [public/index.css, public/app.js]
autonomous: true
requirements: ["다양한 에러 상황 대응 및 모바일 반응형 디자인 점검"]
must_haves:
  truths:
    - "URL 미입력, 잘못된 URL 등의 예외 상황을 프론트엔드에서 1차 차단 및 에러 표시함"
  artifacts:
    - path: "public/app.js"
      provides: "입력값 검증 로직"
  key_links: []
---

<objective>
입력된 URL 검증 로직을 추가하고 에러 발생 시 UI 흔들림 등을 다듬습니다.

Purpose: 사용자가 잘못된 입력을 했을 때 즉각적으로 알려주어 서버의 불필요한 연산을 막고, 사용성을 극대화하기 위함입니다.
Output: 프론트엔드 정규식 검증 로직 추가 및 UI 디테일 폴리싱.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: 유튜브 URL 정규식 검증 추가</name>
  <files>public/app.js</files>
  <read_first>public/app.js</read_first>
  <action>`public/app.js`에서 폼 제출 즉시 입력된 URL이 올바른 유튜브 링크 형식(`youtu.be` 또는 `youtube.com`)인지 판별하는 정규표현식(RegEx) 검사를 추가합니다. 조건에 맞지 않으면 서버로 요청을 보내지 않고 에러 메시지를 띄우고 종료합니다.</action>
  <verify>grep youtube public/app.js exits 0</verify>
  <acceptance_criteria>
    - 잘못된 유튜브 링크 제출 시 경고 메시지 표시 및 fetch 중단됨
  </acceptance_criteria>
  <done>프론트엔드 유효성 검사 추가 완료</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] '가나다라' 같은 엉뚱한 텍스트 입력 시 요청이 가지 않고 에러가 뜸
</verification>

<success_criteria>
- 예외 상황에 완벽하게 대비함
</success_criteria>
